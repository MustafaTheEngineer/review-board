type Tag {
  id: ID!
  name: String!
}

extend type Query {
  tags(query: TagsInput): [Tag!]!
    @checkUsername
    @checkIfConfirmed
    @validateToken
  itemTags(id: ID!): [Tag!]! @checkUsername @checkIfConfirmed @validateToken
}

input TagsInput {
  limit: Int
  offset: Int
  like: String
}
